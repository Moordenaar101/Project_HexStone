// main.cpp pseudocode

#include <Arduino.h>
#include "Utils.h"
#include "LegManager.h"
#include "State.h"
#include "Config.h"
#include "NRF.h"
#include "RuntimeVariables.h"

// Create state objects for each robot state
// Initialize pointers for current and previous state
// Initialize previous loop time

void setup()
{
    // Begin serial communication
    // Attach servos
    // Setup NRF communication
    // Load calibration offsets
    // Set initial state to initialization
    // Call init on current state
    // Switch to sleep state
}

void loop()
{
    // Get current time and elapsed time
    // If not enough time has passed, return
    // Update previous loop time

    // Receive NRF data and update runtime variables

    // If state has changed:
    //     Call exit on previous state
    //     Call init on new state
    //     Update previous state

    // Call loop on current state

    // State Manager Logic

    // If controller not connected:
    //     Set state to sleep
    //     Return

    // If controller is on homepage:
    //     Map joystick values to -100 to 100
    //     Detect if movement is present

    //     If bumper A pressed:
    //         Update last movement time
    //         If not already in gyro state:
    //             If not standing, go to standing
    //             Else, go to gyro state

    //     Else if bumper C pressed:
    //         Update last movement time
    //         If not already in gyro fixed state:
    //             If not standing, go to standing
    //             Else, go to gyro fixed state

    //     Else if movement detected:
    //         Update last movement time
    //         If sleeping, go to standing
    //         Else if not walking, go to walking

    //     Else:
    //         Calculate time since last movement
    //         If not sleeping and time >= stand threshold:
    //             Go to standing
    //         Else if time >= sleep threshold and not sleeping:
    //             Go to sleep

    // If controller is in menu:
    //     If not calibrating or sleeping, go to sleep
    //     If calibrating, go to calibration state
    //     If finished calibrating, go to sleep
}